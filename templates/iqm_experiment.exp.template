********** EXPERIMENT NAME
{{name}}

********** EXPERIMENT NOTES


********** EXPERIMENT INITIAL PARAMETER AND STATE SETTINGS
{{#parameters}}
{{varname}} = ({{upper_bound}} - {{lower_bound}})*rand(1) + {{lower_bound}}
{{/parameters}}
{{#states}}
{{varname}} = ({{upper_bound}} - {{lower_bound}})*rand(1) + {{lower_bound}}
{{/states}}

********** EXPERIMENT PARAMETER CHANGES

********** EXPERIMENT STATE CHANGES
